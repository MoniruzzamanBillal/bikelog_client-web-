# Progress Tracker

Update this file after every spec — status table, Recent Activity log, and Known Gaps as they change. This is the single source of truth for what's actually built vs. planned.

## Current Phase

Specs 01 (project setup), 02 (auth), and 03 (bike management) are complete. The context docs (this set of 6 files) and the spec-by-spec implementation plan (`context/specs/`) were written first, grounded directly in the completed `bikelog_server` API (see `bikelog_server/postman/` for the verified route/schema reference used) and an audit of the inherited Next.js scaffold's existing component library.

Spec 01 turned that scaffold into a clean base: dead shop-admin/table-demo code removed, `lib/tokenManager.ts` + `utils/axiosInstance.ts` + `utils/api.ts`/`hooks/useApi.ts` rebuilt to the documented shapes, `components/layout/AppShell.tsx` + `app/(main)/layout.tsx`'s hard session gate built, `app/page.tsx` now unconditionally redirects to `/login`, and no files exist yet under `app/(admin)/`.

Spec 02 built the actual `/login` and `/register` pages on top of that: zod-validated `LoginForm`/`RegisterForm` (inline `usePost()`, no domain hook wrapper — the deliberate auth exception), each linking to the other, an "already logged in" redirect on both pages, and a logout button wired into `AppShell`. Verified live against the running `bikelog_server` (register, duplicate-email 409, login, wrong-password 403) via direct `curl` calls rather than a browser, since no interactive browser tool is available in this environment.

Spec 03 built the first real screens under `app/(main)/`: `/dashboard` (bike list, card grid, create modal) and `/bikes/[bikeId]` (bike hub — info, edit, delete-with-confirm, four link cards to fuel-logs/mileage/maintenance-logs/spending, none of which have pages yet). This is the first spec where spec 01's session gate and spec 02's logout button had a real protected page to actually run against — confirmed via `curl` that both new routes withhold their content (empty pre-hydration HTML) when no session cookie is present, matching the gate's designed behavior. `components/feature/bike/` is the first populated `components/feature/<domain>/` folder, establishing the pattern (`*.types.ts`, `use<Domain>.ts`, `*FormModal.tsx`) that specs 04–08 will each repeat for their own domain.

This is explicitly a **v1 web prototype**, not the end-state frontend — the developer's actual main target is a React Native app, built after this web version proves the product out. See `project-overview.md`'s Overview for the full framing.

The auth/token handling (`lib/tokenManager.ts`, axios interceptors) and the API-calling/mutation layer (`hooks/useApi.ts`, `utils/api.ts`) were both rebuilt from scratch in spec 01/02 rather than kept from the `table` scaffold's versions — see `architecture.md`'s "Auth & Access Model" and "API Calling & Mutation Pattern" for the exact shapes. On top of that, the session model enforces a **hard navigation gate**: no route besides `/login`/`/register` renders without a valid session, checked synchronously at `app/(main)/layout.tsx` (not left to an API call failing first) — see `architecture.md` Invariant 7.

The backend has two roles (`user`/`admin`) but no role-based authorization implemented yet. v1 of this frontend builds the `user`-role experience only, under `app/(main)/`; `app/(admin)/` is reserved (no files) for an admin panel later — see `project-overview.md`'s "Roles" section and `architecture.md` Invariant 8.

## Spec Implementation Status

| Spec                                                                                                       | Status         | Notes                                                                                                                            |
| ---------------------------------------------------------------------------------------------------------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| [00-build-plan.md](specs/00-build-plan.md)                                                                 | N/A (index)    | Lists all specs and build order.                                                                                                 |
| [01-project-setup.md](specs/01-project-setup.md)                                                           | ✅ Complete    | Cleanup + token manager/interceptors + hard session gate + app shell — must go first, everything else depends on it.             |
| [02-auth.md](specs/02-auth.md)                                                                             | ✅ Complete    | Register/login pages (zod-validated, single-token, no refresh), each linking to the other.                                       |
| [03-bike-management.md](specs/03-bike-management.md)                                                       | ✅ Complete    | Bike list, create/edit modal, bike hub page.                                                                                     |
| [04-fuel-logs.md](specs/04-fuel-logs.md)                                                                   | ✅ Complete    | Fuel log CRUD.                                                                                                                   |
| [05-mileage-stats.md](specs/05-mileage-stats.md)                                                           | ✅ Complete    | History/Monthly/Yearly/Lifetime tabs.                                                                                            |
| [06-maintenance-catalog.md](specs/06-maintenance-catalog.md)                                               | ✅ Complete    | Maintenance-type + engine-oil-type create/list.                                                                                  |
| [07-maintenance-logs-and-reminders.md](specs/07-maintenance-logs-and-reminders.md)                         | ✅ Complete    | Maintenance log CRUD + reminders banner.                                                                                         |
| [08-spending-summary.md](specs/08-spending-summary.md)                                                     | ✅ Complete    | Spending totals + category breakdown.                                                                                            |
| [10-audit-fixes.md](specs/10-audit-fixes.md)                                                               | ✅ Complete    | Fix pass over specs 04-08: 9 functional bugs + 2 type-safety cleanups + doc sync.                                                |
| [11-bike-issue.md](specs/11-bike-issue.md)                                                                 | ✅ Complete    | Bike issue tracking (open/resolved lifecycle) — client for backend spec 10.                                                      |
| [12-bike-accessory.md](specs/12-bike-accessory.md)                                                         | ✅ Complete    | Bike accessory purchase wishlist — client for backend spec 11.                                                                   |
| [13-spending-and-mileage-trend-charts.md](specs/13-spending-and-mileage-trend-charts.md)                   | ✅ Complete    | Trend tab/view for Spending + Mileage using Recharts — client for backend spec 15.                                               |
| [14-ai-integration.md](specs/14-ai-integration.md)                                                         | ✅ Complete    | AI spending/mileage insight cards + bike chat page — client for backend spec 16.                                                 |
| [15-image-uploads.md](specs/15-image-uploads.md)                                                           | ✅ Complete    | Client for backend spec 17 — receipt/service/product/issue-evidence image upload.                                                |
| [16-image-lightbox.md](specs/16-image-lightbox.md)                                                         | ✅ Complete    | Full-screen image lightbox on top of spec 15's uploaded images — no backend changes.                                             |
| [17-bike-documents.md](specs/17-bike-documents.md)                                                         | ✅ Complete    | Bike papers/IDs with expiry tracking, mixed image+PDF files — client for backend spec 19.                                        |
| [18-bike-manual.md](specs/18-bike-manual.md)                                                               | ✅ Complete    | Owner manual PDF upload/replace/delete (one per bike) — client for backend spec 18 (already complete).                           |
| [19-decimal-number-field-fix.md](specs/19-decimal-number-field-fix.md)                                     | ✅ Complete    | Added `step="0.01"` to odometer/interval/cost/fuel-tank-capacity inputs so decimals aren't blocked by native HTML5 validation.   |
| [20-trend-charts-6-month-window.md](specs/20-trend-charts-6-month-window.md)                               | ✅ Complete    | Widen Spending/Mileage trend charts (spec 13) to a 6-month window — client for backend spec 22.                                  |
| [21-spending-pdf-export.md](specs/21-spending-pdf-export.md)                                               | ✅ Complete    | Spending details PDF export — client for backend spec 23.                                                                        |
| [22-bug-fixes-maintenance-type-and-form-issues.md](specs/22-bug-fixes-maintenance-type-and-form-issues.md) | ✅ Complete    | Bug fixes: maintenance-type display, accessory price validation, stale form state across 6 modals, `usePatch` config forwarding. |
|                                                                                                            |

## Completed (already implemented)

- **Spec 01 — Project Setup**: scaffold cleanup (removed TipTap/GSAP/table-demo/`js-cookie` code and deps), `lib/tokenManager.ts`, rebuilt `utils/axiosInstance.ts` auth interceptors (no refresh-token flow), rebuilt `utils/api.ts`/`hooks/useApi.ts` (`useFetchData`/`usePost`/`usePatch`/`useDelete`), `components/layout/AppShell.tsx`, `app/(main)/layout.tsx` hard session gate, `app/page.tsx` unconditional `/login` redirect, `globals.css` token/cleanup fixes. See spec's own Verify checklist for exact status of each item.
- **Spec 02 — Auth**: `components/feature/auth/auth.schema.ts` (`loginSchema`/`registerSchema`), `LoginForm.tsx`/`RegisterForm.tsx` (zod + `zodResolver`, inline `usePost()`, show/hide password toggle), `app/login/page.tsx`/`app/register/page.tsx` (thin wrappers + already-logged-in redirect), logout wired into `AppShell`. `components/shared/input/ControlledInput.tsx` gained an optional `rightElement` slot (extended, not forked) to support the password eye-icon toggle. See spec's own Verify checklist for exact status of each item.
- **Spec 03 — Bike Management**: `components/feature/bike/bike.types.ts` (`TBike`/`TCreateBikePayload`/`TUpdateBikePayload`), `useBikes.ts` (`useBikes`/`useBike`/`useCreateBike`/`useUpdateBike`/`useDeleteBike`), `BikeFormModal.tsx` (create+edit, `currentOdometer` hidden on edit), `BikeCard.tsx`, `app/(main)/dashboard/page.tsx` (card grid), `app/(main)/bikes/[bikeId]/page.tsx` (hub page with edit/delete + four link cards). See spec's own Verify checklist for exact status of each item.
- **Spec 16 — Image Lightbox**: new `components/shared/ImageLightbox/ImageLightbox.tsx` (full-viewport `Dialog` override, `object-contain` `next/image`, `ChevronLeft`/`ChevronRight` nav with disabled-at-bounds chevrons and a counter overlay, keyboard arrow nav, backdrop-click-to-close), wired self-contained into `ImageGalleryField.tsx` (thumbnail click opens lightbox at that index) and `ImageUploadThumb.tsx` (thumbnail click opens lightbox when an image is set; a new `Pencil` overlay button next to the delete `X` now owns "replace"). No changes needed to the 4 domain components. See spec's own Verify checklist for exact status of each item.

## Recent Activity

- **2026-08-25 (spec 22 implementation)** — Executed `context/specs/22-bug-fixes-maintenance-type-and-form-issues.md` per direct user request, scope limited strictly to spec 22 (a bundled 4-bug fix pass found during an earlier source-level bug-hunt, distinct from and not touching the still-open `RemindersBanner.tsx` maintenance-type gap below). All 7 Implementation steps completed exactly per the spec's own Design section, no deviations: (1) `MaintenanceLog.tsx` now fetches `/maintenance-types` (same `useFetchData<TMaintenanceType[]>` pattern already used by `MaintenanceLogFormModal.tsx`) and passes the list to `MaintenanceLogCard`; (2) `MaintenanceLogCard.tsx`'s `getTypeName` gained a string-`maintenanceType` branch that resolves the id against that list before falling back to the literal `"Maintenance"`; (3) `bike-accessory.schema.ts`'s two `price` refines now short-circuit on a falsy/empty value (`!val || ...`) instead of running `Number()`/`isNaN()` against `undefined`; (4) all 5 domains' "Add" modals (`FuelLog.tsx`, `BikeIssue.tsx`, `BikeAccessory.tsx`, `BikeDocument.tsx`, `MaintenanceLog.tsx`) plus `BikeDetailPage.tsx`'s Edit `BikeFormModal` are now conditionally rendered (`{openState && <XFormModal open .../>}`) instead of always-mounted with `open` toggled via prop, matching the pattern every domain's own Edit modal already used correctly; (5) `utils/api.ts`'s `apiPatch` now accepts and forwards an optional `config` param, and `hooks/useApi.ts`'s `usePatch` forwards `params.config` through to it, matching `apiPost`/`usePost`'s existing shape. Verified: `yarn lint` — 0 errors, same 5 pre-existing warnings as baseline (`MaintenanceLogFormModal.tsx`'s `watch()` compiler-skip, `FuelLog.tsx`'s unused `isPending`, the two `useReactTable()` compiler-skips, `TableToolbar.tsx`'s unused `activeTab`), none new; `yarn build` — succeeds, all 15 routes compile including a full `tsc` type-check. No interactive browser tool is available in this session, so the actual click-through (typed-then-cancelled form values not resurfacing on reopen, a blank-price accessory actually submitting, the maintenance log list showing a real type name instead of the literal string) is code-reviewed against the fix and the already-confirmed backend/type contract, not literally exercised — same standing limitation noted throughout this file. Spec 22's own Status/Implementation/Verify sections updated in place; this file's own status table and this entry are the required doc-sync.

- **2026-08-17 (spec 17 implementation, verification-only — code already existed)** — Executed `context/specs/17-bike-documents.md` per direct user request, scope limited strictly to spec 17, client for the already-complete `bikelog_server` spec 19 (`bikeDocument` module). On starting, found that all 9 required files (`bike-document.types.ts`, `bike-document.schema.ts`, `FileGalleryField.tsx`, `BikeDocumentFormModal.tsx`, `BikeDocumentCard.tsx`, `BikeDocument.tsx`, `app/(main)/bikes/[bikeId]/documents/page.tsx`, `BikeDetailPage.tsx`'s "Documents" tile, and `ControlledDateSelect.tsx`'s optional `placeholder` prop) were already written on disk, matching the spec's Design section field-for-field — a prior, uncommitted-to-docs pass had done the actual coding but never run `yarn build`/`yarn lint`, never live-verified it, and never updated the spec's own checkboxes or this file, leaving it stuck at 🔨 In Progress with an unexplained gap between real code and real docs. First re-verified the backend contract directly against `bikelog_server` source (routes, `TCloudinaryFile` field names, the `getBikeDocumentsFromDB` two-group-concat expiry-first sort) rather than trusting the spec's own Context section's assumptions, per the spec's own explicit instruction to do so — found zero deviations, unlike spec 15's own history of catching one.

  `yarn build` succeeds (all 16 routes compile, including `/bikes/[bikeId]/documents`); `yarn lint` shows only the same pre-existing 5-warning/0-error baseline, none new — **no code changes were needed**, the existing implementation was correct as written. Extensively exercised live in a real headless-Chromium browser (Playwright, 390px viewport) against a temporary local `bikelog_server` instance with a throwaway user/bike (this session has real browser-testing capability, unlike whenever this spec's own Verify checklist was originally drafted with "no interactive browser tool" caveats baked in): created a title-only document (no badge, correct); created three more with past/near/far expiry dates and confirmed all three badge colors/labels exactly ("Expired" red, "Expires in 19 days" amber, "Expires 25-Dec-2026" neutral) with the list correctly sorted soonest-expiry-first then no-expiry-last; uploaded a real 1×1 PNG + a real minimal PDF to one document in a single request against real Cloudinary and confirmed two distinct tiles (image thumbnail vs. `FileText`-icon + truncated filename, the PDF tile a real `target="_blank"` link to the actual Cloudinary raw URL); deleted just the PDF tile and confirmed via direct API + a direct HTTP fetch of its old Cloudinary URL (`404`) that only that asset was destroyed while the image stayed live; selected 10 files against a document that already had 1 (9 slots remaining) and got the exact expected toast ("Only 9 more files can be added right now (max 10 total) — queued the first 9.") with a final tile count capped at exactly 10; deleted a whole document via its trash icon + the real native `confirm()` dialog (intercepted and accepted, not bypassed) and confirmed it dropped out of the list while the others stayed; confirmed a second real throwaway user got `404` listing the first user's bike's documents (IDOR check); confirmed no horizontal overflow at 390px (`scrollWidth === clientWidth`) throughout. The edit-never-touches-`files[]` checklist item was confirmed by code review only (the payload shape makes it structurally impossible, not just untested). All fixtures (1 bike, 5 documents, 2 users, 11 uploaded test files) were cleaned up afterward — the bike soft-deleted through the app's own `DELETE /bikes/:id` endpoint first (destroying its documents' Cloudinary assets along the way, per the backend's own best-effort cleanup), both users hard-deleted via a temporary in-tree backend script (reusing `bikelog_server`'s own `config`/`mongoose.connect`, deleted immediately after running). Spec 17's own Status/Implementation/Verify sections updated in place to reflect exactly what was verified and how.

- **2026-08-17 (spec 21 re-verification, no code changes)** — Per direct user request to re-verify spec 21 after it was already merged (commit `fe3b687`), re-ran the full flow end-to-end against fresh live data: `yarn build`/`yarn lint` both clean (same pre-existing 5-warning/0-error baseline); a new throwaway user/bike/maintenance-type with 2 fuel logs + 1 maintenance log; downloaded and `pdftotext`-verified all three PDFs (`spending-month-2026-08.pdf` — Tk 1,358 total, 3 correctly-ordered line items, `-`/`-` for the fuel log missing `fuelStation`/`notes`, no mojibake anywhere; `spending-year-2026.pdf`; `spending-lifetime.pdf`); cross-checked the PDF's total against a live `GET /spending-summary` call for the same period and confirmed an exact match (`1358` both places — no drift); re-confirmed the Export PDF button's presence/absence on Month vs. Trend; re-exported a genuinely zero-record month (`2019-05`) and confirmed a real PDF with `Total Spending: Tk 0` + "No spending records for this period." (not a silent no-op); re-triggered the ownership-error path against a nonexistent bike id and confirmed the `sonner` toast still surfaces the backend's real message ("Bike not found") with the button re-enabled afterward. `git status` was clean both before and after — this pass touched no source files, only confirmed the already-merged implementation still behaves exactly as spec 21's own Verify checklist describes. All fixtures cleaned up the same way as the original implementation pass.

- **2026-08-17 (spec 21 implementation)** — Executed `context/specs/21-spending-pdf-export.md` per direct user request, scope limited strictly to spec 21, client for the now-complete `bikelog_server` spec 23 (Spending Details Export Endpoint). Added `TSpendingRecordSource`/`TSpendingRecord`/`TSpendingDetails` to `type/spending.types.ts`, mirroring the backend's field names exactly. Installed `jspdf@4.2.1` + `jspdf-autotable@5.0.8` (this app's first PDF-generation dependency) after confirming `jspdf-autotable@5`'s declared peer range accepts `jspdf@4`. New `components/(main)/Spending/utils/generateSpendingPdf.ts` builds the PDF client-side (header → summary → category-breakdown table → line-item table, via `jspdf-autotable`), matching the spec's Design layout. `Spending.tsx` gained `isExporting` state, a `handleExportPdf` that reuses the same `searchParams` already built for the on-screen summary fetch (just pointed at `/spending-summary/details` instead of `/spending-summary`, via a direct one-off `apiGet` call — not `useFetchData`'s auto-fetching `useQuery`, since this is an on-demand action), and an "Export PDF" button (icon + label, `Loader2`+"Exporting..." while pending, `sonner` `toast.error` with the backend's real message on failure) added to the controls row alongside the Month/Year picker and unconditionally for Lifetime — hidden entirely on the Trend tab, matching the plain-`<button>`+Tailwind idiom this file already uses for its other controls rather than introducing the shared `Button` component.

  **Real deviation from the spec's literal design, discovered during implementation, not a scope change:** the spec's Design section calls for `৳`-prefixed amounts throughout the PDF, matching the rest of the app — but jsPDF's built-in fonts (Helvetica/Times/Courier) only cover WinAnsi/Latin-1, so `৳` (U+09F3) rendered as mojibake (`ó`) in the first working version. Tried embedding a Bengali-script TTF (`Noto Sans Bengali`, SIL OFL, available on this system) via `jsPDF.addFont()`/`addFileToVFS()` — this made things _worse_, not better: an isolated standalone Node test (same installed `jspdf` package, run outside the Next app/browser entirely) showed jsPDF's TTF parser breaks badly enough on this specific font file that it silently drops **all** text, plain Latin included, not just Bengali glyphs. Confirmed this was a jsPDF/font-file interaction problem and not an environment or app issue by successfully embedding a different TTF (`DejaVuSans`) in the same isolated test — that one rendered Latin text correctly but (confirmed via `fc-query`'s charset dump beforehand) doesn't contain the `৳` glyph at all, so it silently drops just that one character instead of breaking everything, which was still the wrong output for a different reason. Landed on the safe, working fallback: PDF-only `"Tk"` ASCII prefix (a `CURRENCY_PREFIX` constant + a `sanitizeForPdf()` helper in the one new file, confined there — nothing else changed), applied to every amount column _and_ to `record.description` (a backend-synthesized string, e.g. `"5L (Full Tank) @ ৳100/L"` from `bikelog_server` spec 23, which itself contains a literal `৳` that would otherwise leak the same mojibake into the line-item table). The live on-screen UI is completely untouched and still shows `৳` everywhere via ordinary browser font rendering — this was never broken; the limitation is specific to jsPDF's own text-rendering engine for the exported file only.

  Verified: `yarn build` succeeds (all 15 routes compile); `yarn lint` shows only the same pre-existing 5 warnings / 0 errors baseline, none new. **Extensively exercised live in a real headless-Chromium browser** (Playwright, 390×844 mobile viewport) against a temporary local `bikelog_server` instance with a throwaway user/bike/maintenance-type and 3 real log entries (2 fuel — one with `fuelStation`/`notes` set, one without — plus 1 maintenance log): downloaded all three PDFs (`spending-month-2026-08.pdf`, `spending-year-2026.pdf`, `spending-lifetime.pdf`) via Playwright's `expect_download`, then read each file's actual text with `pdftotext` (not just confirming a download fired) — header/period-label/timestamp all correct, `Total Spending: Tk 1,170` and the category table matching the on-screen `SpendingSummaryView` exactly, 3 line-item rows in correct ascending-date order, `Vendor`/`Remarks` showing `-` (not blank or `"null"`) for the fuel log missing those fields, dates human-formatted (`5 Aug 2026` etc.); confirmed the Export PDF button is present on Month/Year/Lifetime and absent on Trend via a live DOM query; exported a zero-record month (`2020-01`) and confirmed via `pdftotext` it still produced a real PDF with `Total Spending: Tk 0` and the literal "No spending records for this period." line, not a silent no-op; triggered a real backend error (navigated to a nonexistent bike id) and confirmed a `sonner` toast surfaced the backend's actual message ("Bike not found") with the button re-enabled afterward (not stuck disabled); confirmed no horizontal overflow at 390px (`scrollWidth === clientWidth` on all three non-trend tabs) and screenshotted the Year tab specifically (chevrons + year + Export PDF button all in one row) as the tightest layout case, which fit with room to spare. All fixtures (1 bike, 1 maintenance type, 2 fuel logs, 1 maintenance log, 1 throwaway user) were cleaned up afterward — the bike soft-deleted through the app's own `DELETE /bikes/:id` endpoint first, the user hard-deleted via a temporary in-tree backend script (reusing `bikelog_server`'s own `config`/`mongoose.connect`, deleted immediately after running, same pattern as that project's own cleanup passes). Spec 21's own Status/Implementation/Verify sections updated in place to reflect exactly what was built, the currency-symbol deviation, and how everything was verified. This was the last outstanding spec depending on `bikelog_server` spec 23 — no other work is blocked on it.

- **2026-08-17 (spec 20 implementation)** — Executed `context/specs/20-trend-charts-6-month-window.md` per direct user request, scope limited strictly to spec 20. Widened the Spending and Mileage trend charts (shipped in spec 13) from a hardcoded last-3-months window to a hardcoded last-6-months window — a client-only change, since the backend trend endpoints already accept an arbitrary `?months=N` (`bikelog_server` spec 15, re-verified for `N=6` in its own spec 22, implemented the same day). Four literal-value edits, no structural changes, exactly as the spec's own Design table called for: `SpendingTrendChart.tsx:37` (`?months=3` → `?months=6`) and `:54` (label → `"Spending, last 6 months"`); `MileageTrendTab.tsx:26` (`?months=3` → `?months=6`) and `:38` (label → `"Distance, last 6 months"`). No changes to `type/spending.types.ts`, `type/mileage.types.ts`, `Spending.tsx`, or `Mileage.tsx` — the spec-13 `"trend"`/`"trends"` tab wiring was untouched. Verified: `yarn build` succeeds (all 15 routes compile); `yarn lint` shows only the same 5 pre-existing warnings, 0 errors, none new in either touched file. **Visually confirmed live in a real headless-Chromium browser** (Playwright, this session's environment has this capability where several prior specs' entries note it didn't) at a 390×844 mobile viewport: temporarily pointed `utils/config/envConfig.ts` at a local `bikelog_server` instance (reverted immediately after, confirmed via `git diff` showing zero net change to that file), registered a throwaway user/bike with fuel logs dated Mar–Aug 2026, and screenshotted both the Spending "Trend" tab and Mileage "Trends" tab — both rendered exactly 6 bars with correct headers (`"Spending, last 6 months"` / `"Distance, last 6 months"`) and clean, non-overlapping `"Mar"`–`"Aug"` axis labels; no tick-angling or font-size changes were needed, matching the spec's "don't preemptively adjust, verify first" guidance. One test-harness-only false alarm along the way: a `full_page` Playwright screenshot of the Mileage chart initially appeared to show no bars at all, but the underlying SVG (queried directly, and confirmed against the live `GET .../mileage/trend?months=6` response) had the correct geometry and fill the whole time — a cropped element-screenshot after properly waiting for `.recharts-bar-rectangle path` to exist rendered correctly, confirming this was a screenshot-timing artifact in the test script, not an app bug. The category-breakdown donut was confirmed still tied to only the most recent month (`"By category (Aug 2026)"` in the live screenshot), unaffected by the window widening. All throwaway fixtures (1 bike, 6 fuel logs, 1 user) were cleaned up afterward — the bike soft-deleted through the app's own `DELETE /bikes/:id` endpoint, the user hard-deleted via a temporary in-tree backend script (reusing `bikelog_server`'s own `config`/`mongoose.connect`, deleted immediately after running, same pattern as that project's own cleanup passes). Spec 20's own Status/Implementation/Verify sections and `context/specs/00-build-plan.md`'s index were both updated in place; the latter also surfaced (but deliberately did not backfill, as out of scope for this spec) a pre-existing gap where rows 11–19 were never added to that index despite being long-since `✅ Complete` per this file's own table.

- **2026-08-06 (spec 19 implementation)** — Executed `context/specs/19-decimal-number-field-fix.md` per direct user request, after the user reviewed the spec. Fixes a real, user-reported bug: typing a decimal odometer reading (e.g. `408.8`) into "Add Fuel Log" was blocked by the browser's native HTML5 constraint validation, which defaults a `type="number"` input's `step` to `1` (whole-number-only) whenever the `step` prop is omitted — confirmed via `ControlledInput.tsx` forwarding `step` straight to the native `<input>`, and via `litersAdded`/`pricePerLiter` already working correctly because they already passed `step="0.01"`. None of the affected fields' Zod schemas (`fuel-log.schema.ts`/`maintenance-log.schema.ts`/`bike.schema.ts`) impose any integer restriction — this was purely a presentation-layer gap, not a validation-intent mismatch, and the backend (`bikelog_server`'s own spec 20, executed the same session) was independently confirmed to have no integer constraint either. Added `step="0.01"` to 6 `ControlledInput` call sites across 3 files: `FuelLogFormModal.tsx`'s `odometerReading`; `MaintenanceLogFormModal.tsx`'s `odometerReading`, `intervalKmUsed`, `cost`; `BikeFormModal.tsx`'s `fuelTankCapacityLiters`, `currentOdometer`. No schema, type, hook, or backend change — confined entirely to the three form-modal JSX files, exactly as the spec's own Dependencies section predicted. Verified: `yarn lint` — 0 errors, same 5 pre-existing warnings, none new; `yarn build` — succeeds, all 15 routes compile including the 3 touched dynamic routes. No interactive browser tool is available in this environment, so the actual click-through (typing `408.8`, confirming no native validation popup, confirming the submitted/round-tripped value) is code-reviewed against the confirmed root cause, not literally observed — same standing limitation as every prior spec in this file. Spec 19's own Status/Implementation/Verify sections updated to reflect what was actually done.

- **2026-07-30 (spec 18 implementation)** — Built spec 18 (owner manual PDF upload/replace/delete, one per bike, client for backend spec 18 which was already complete) end-to-end, per the spec's own 5-step Implementation checklist (all completed). New `components/(main)/BikeManual/type/bike-manual.types.ts` (`TBikeManualMeta`, `TBikeManualStatus`), and a single new `components/(main)/BikeManual/BikeManual.tsx` — deliberately **not** built on top of `ImageUploadThumb.tsx` or `FileGalleryField.tsx` (spec 15/17), since both are small tile-grid components (image-thumbnail or multi-file-gallery shaped) and this feature is a single full-width metadata card (filename, formatted upload date, `chunkCount` — the concrete signal the manual was actually indexed for AI chat) with no existing shared component fitting that shape; closest structural precedent was `RemindersBanner.tsx`'s self-contained `bikeId`-prop-driven card. Empty state is a dashed-border upload button; once a manual exists, shows metadata plus "View PDF" (opens `manual.url` in a new tab, no in-app PDF viewer — same convention spec 17 established), "Replace" (re-`POST`s to the same upsert endpoint — confirmed via direct read of `bikeManual.route.ts` that this module has **no `PUT` route**, unlike spec 15's image uploads, so `usePost` is used for both first upload and replace, deliberately not `usePut`), "Delete" (via the existing shared `ConfirmDeleteModal`), and a closing link to `/bikes/:bikeId/assistant` ("Ask the AI Assistant about this manual") since that's the entire point of the upload — backend spec 18 already grounds that existing chat endpoint in the manual, this client change is the only missing piece. New route `app/(main)/bikes/[bikeId]/manual/page.tsx` (same 4-line wrapper shape as every other bike-scoped page). `BikeDetailPage.tsx`'s `links` array gained a 9th "Manual" tile (`BookOpen` icon, previously unused in this codebase), alongside spec 17's "Documents" tile. No new npm packages — reused `date-fns` (upload-date formatting, `"dd-MMM-yyyy"` matching `BikeIssueCard.tsx`'s existing format string), `lucide-react`, and the existing `usePost`/`useDelete`/`useFetchData` hooks. No `use<Domain>.ts` hook file, matching the established convention from `BikeIssue`/`BikeAccessory`/`BikeDocument` (self-contained mutations inline in the component, no per-domain hook wrapper for these newer specs). `yarn build` succeeds (all routes compile, emits `/bikes/[bikeId]/manual` as a new dynamic route); `yarn lint` exits 0 (same 5 pre-existing warnings as spec 17's baseline, none new). No interactive browser tool is available in this environment, and no local `bikelog_server` instance was reachable during this implementation (`lsof` confirmed ports 3000/5000/5173 all free, unlike several prior specs that verified live via `curl`) — so unlike those specs, this one's Verify checklist is code-reviewed against the backend contract read directly from source (`bikeManual.route.ts`/`.controller.ts`/`.service.ts`) rather than confirmed via live API round-trips; worth a live pass once the backend is running. Spec 18's own Status/Implementation/Verify sections were all updated to reflect what was actually built and how it was verified.

- **2026-07-28 (spec 16 implementation)** — Built spec 16 (full-screen image lightbox, pure display feature on top of spec 15's already-uploaded images) end-to-end, per the spec's own 5-step Implementation checklist (all completed). This spec was written "review-first" — its four Open Questions were left unresolved by the user — so the decisions applied here, treated as settled per explicit instruction rather than re-asked, are the historical record now baked into the spec's own Status/Verify sections: (1) **Option A** for the `ImageUploadThumb` click-target conflict — clicking an already-set thumbnail now opens the lightbox instead of the file picker, with "replace image" moved to a new small `Pencil` overlay button (same visual treatment as the existing delete `X`, both `stopPropagation`'d so neither also triggers the lightbox or each other) — the no-image placeholder state is untouched and still opens the file picker on click; (2) **no wraparound** — the `ChevronLeft`/`ChevronRight` nav buttons `disabled` (not hide) at the first/last image, mirroring `TablePagination.tsx`'s exact `disabled:opacity-50 disabled:cursor-not-allowed`-style convention (adapted to `disabled:opacity-30` for a dark backdrop) rather than inventing a new one; (3) pinch-zoom left out of scope — `object-contain` fit-to-screen only; (4) `ArrowLeft`/`ArrowRight` keydown nav added (window-level listener, only attached while `open`, cleaned up on unmount/close), on top of `Escape`-to-close which came free via Radix `Dialog`. New `components/shared/ImageLightbox/ImageLightbox.tsx` builds directly on `Dialog`/`DialogContent` from `components/ui/dialog.tsx` (`showCloseButton={false}`, a full-viewport override className replacing the default centered `sm:max-w-lg` card — including an explicit `sm:max-w-dvw` correction, since `tailwind-merge` only strips the exact `max-w-*` utility class it's given a replacement for, not the responsive-prefixed `sm:max-w-lg` variant separately), dark `bg-black/90` backdrop, backdrop-click-to-close implemented as `onClick={onClose}` on the outer wrapper with `onClick={(e) => e.stopPropagation()}` on the image's own wrapper (Radix's built-in outside-click behavior doesn't fire correctly once `DialogContent` is stretched to fill the viewport, exactly as the spec's Design section flagged). `ImageGalleryField.tsx` and `ImageUploadThumb.tsx` each own their lightbox's open/index state locally — consistent with how both already self-manage their own `ConfirmDeleteModal` state — so, as the spec predicted, none of the 4 domain integration points (`FuelLogReceiptCell.tsx`, `MaintenanceLogCard.tsx`, `BikeAccessoryCard.tsx`, `BikeIssueCard.tsx`) needed any changes. One implementation deviation from the spec's literal snippet, not a scope change: the spec's proposed `useEffect(() => { if (open) setCurrentIndex(initialIndex) }, [open, initialIndex])` trips `react-hooks/set-state-in-effect` as a lint **error** (calling `setState` synchronously inside an effect body) — rather than suppress it, used the same React-documented "adjust state during render" pattern spec 14's `AiAssistant.tsx` already established in this codebase for the equivalent case (a `prevResetKey` state value compared against a `` `${open}|${initialIndex}` `` token, with `setCurrentIndex` called in the component body when they differ, not inside an effect). No new npm packages, matching the spec's own "Dependencies: None" — reused `@radix-ui/react-dialog` (via `components/ui/dialog.tsx`), `lucide-react` (`Pencil`, `X`, `ChevronLeft`, `ChevronRight`, already all in use elsewhere except `Pencil`), and `next/image`. `yarn build` succeeds (all 8 routes compile, no new routes since this is a display feature on existing pages); `yarn lint` exits 0 (same 5 pre-existing warnings as spec 15's baseline, none new — the `set-state-in-effect` error above was hit and fixed during this same implementation pass, not left in). No interactive browser tool is available in this environment, so the actual click-through (thumbnail-click-opens-lightbox, chevron/keyboard nav, backdrop-click-to-close, disabled-chevron-at-bounds styling, phone-width layout) is code-reviewed against the spec's Design section and this app's established conventions, not literally exercised — same standing limitation as every prior spec in this file. **Post-implementation correction (same session, before sign-off):** the shipped "click image does not close, click backdrop does" split didn't actually work — the image's direct wrapper `div` had `onClick={(e) => e.stopPropagation()}`, but that div must be `h-full w-full` for `next/image`'s `fill` layout, so it covered the entire viewport and silently swallowed every click in the modal, not just ones on the visible image, leaving the `X` button as the only working way to close it. Fixed by removing that `onClick`/`stopPropagation` entirely, so a click anywhere in the lightbox — including directly on the photo — now bubbles to the outer wrapper's `onClick={onClose}`. Accepted rather than reworked further to preserve the original "image click is a no-op" behavior, since precisely distinguishing "on the letterboxed image pixels" from "on the surrounding area" isn't reliable without measuring the image's actual rendered box, and this matches the equivalent fix and reasoning applied the same day to the mobile app's own image viewer (spec 21). `yarn build`/`yarn lint` re-run clean after the fix (same 5-warning baseline). Spec 16's Design and Verify sections were both updated to describe the shipped mechanism, not the originally-planned one.

- **2026-07-27 (spec 15 implementation)** — Built spec 15 (image uploads, client for backend spec 17) end-to-end, per the spec's own 14-step Implementation checklist (all completed). Closed a real gap flagged in the spec's own Context: neither `utils/api.ts` nor `hooks/useApi.ts` had any `PUT` support before this — added `apiPut` (mirrors `apiPost`'s signature) and `usePut` (mirrors `usePatch`'s shape) to both, since 3 of the 4 new backend routes are `PUT .../image`. Added `res.cloudinary.com` to `next.config.ts`'s `images.remotePatterns` (previously only `i.postimg.cc`/`i.pravatar.cc` were whitelisted — `next/image` would otherwise throw at runtime for every Cloudinary-hosted image). New shared `components/shared/type/image.types.ts` (`TCloudinaryImage`) and two new shared components: `components/shared/input/ImageUploadThumb.tsx` (single-image click-to-upload thumbnail with an `X`-overlay delete and a spinner while pending — reused by fuel logs, maintenance logs, bike accessories) and `components/shared/input/ImageGalleryField.tsx` (bike issue's multi-image variant — thumbnail grid + a dashed "+" add tile, client-side capped at `5 - current count` per the server's own 5-per-request limit, with a `toast.info` if the user selects more than that). Each of the four domains' type files gained its optional image field (`receiptImage`/`serviceImage`/`productImage`/`images`), matching the backend's additive/no-migration schema change. Rather than lifting the upload/delete mutations up to each domain's list-page component (the way edit/delete are already lifted there), every card — and a new `FuelLogReceiptCell.tsx` for the table-based fuel log column (`fuelLogColumns.tsx`'s cells are plain functions, not existing components, so a small wrapper was needed to give the mutation a place to live) — owns its own `usePut`/`usePost`/`useDelete` instance directly, keyed off the record's own `.bike` field rather than threading a new `bikeId` prop through components that don't currently receive one; this mirrors the self-contained-mutation pattern the mobile app's `BikeIssueCard.tsx` already uses for its status toggle. `yarn build` succeeds (all 8 routes compile), `yarn lint` exits 0 (same 5 pre-existing warnings, none new). No interactive browser tool is available in this environment, so the actual click-through (thumbnail rendering, upload/replace/delete round-trips against a live Cloudinary-backed backend, gallery add/remove) is code-reviewed against the confirmed backend contract (spec 17) and this app's established conventions, not literally exercised — same standing limitation as every prior spec in this file. This also makes the Known Gaps entry below about `useApi.ts` having "no `apiPut`... `bikelog_server` has no PUT routes" **stale as of this spec** — the backend now has real `PUT` routes and this spec added the client-side support for them.

- **2026-07-26 (spec 14 implementation)** — Built spec 14 (AI integration: spending insight, mileage insight, bike chat — v2 scope, client for backend spec 16) end-to-end, per the spec's own scoped Implementation list (10 steps, all completed). `components/(main)/Spending/type/spending.types.ts` and `components/(main)/Mileage/type/mileage.types.ts` each gained a `T{Spending,Mileage}Insight` type (`{insight: string; generated: boolean; cached: boolean}`); new `AiSpendingInsightCard.tsx`/`AiMileageInsightCard.tsx` each do their own `useFetchData<T...Insight>(["ai", "...-insight", bikeId], ...)` and render the standard card idiom (`rounded-lg border border-border bg-card p-4`, muted label + body), with a `"Thinking..."` plain-text loading state matching `SpendingSummaryView`'s existing convention — both take `bikeId` as a prop rather than re-deriving it via `useParams()`, matching the established pattern from spec 13's `SpendingTrendChart`/`MileageTrendTab`. `Spending.tsx`/`Mileage.tsx` each render their new card above the existing period-buttons/tab-row. New domain folder `components/(main)/AiAssistant/` (`type/aiAssistant.types.ts` — `TChatMessage`, `TBikeChatResponse`; `AiAssistant.tsx` — reads `bikeId` via `useParams()`, owns the conversation as local `useState<TChatMessage[]>([])`, message bubbles built from scratch (user right-aligned `bg-primary`, assistant left-aligned `bg-card border`), a `Textarea` + icon `Button` input row with Enter-to-send (Shift+Enter for newline), optimistic user-message append, `usePost()` to `/bikes/:bikeId/ai/chat` resending the full history each call, a `Loader2`-spinner "AI is thinking..." bubble while pending, and a `sonner` toast (not a fabricated reply) on mutation failure. New route `app/(main)/bikes/[bikeId]/assistant/page.tsx` (same 4-line wrapper shape as every other bike-scoped page). `BikeDetailPage.tsx`'s `links` array gained a 7th "AI Assistant" tile (`Sparkles` icon, previously unused in this codebase). One implementation deviation from the spec's literal snippet: the spec's `useEffect(() => setMessages([]), [bikeId])` (to avoid leaking one bike's conversation into another's) trips the `react-hooks/set-state-in-effect` lint rule as an **error**, not a warning — rather than suppress it with `eslint-disable` (the precedent set by `app/(main)/layout.tsx`'s session gate and the login/register mount-check for a different, genuinely-unavoidable case), used React's documented render-time state-reset pattern instead: track a `prevBikeId` state value and, in the component body (not an effect), call `setPrevBikeId(bikeId)` + `setMessages([])` when they differ — this is the React-recommended alternative for "reset state when a prop changes" and needed no suppression at all, since it's not actually a side effect. Verified live against the running `bikelog_server` via `curl` (register → login → create bike incl. required `fuelTankCapacityLiters` → create one fuel log → `GET .../ai/spending-insight` twice, confirming the 2nd call returns the identical insight text with `cached: true` flipped from the 1st call's `cached: false` → `GET .../ai/mileage-insight`, confirming the response shape → two-turn `POST .../ai/chat` with the full history resent on the 2nd call, confirming the reply correctly used the 1st turn's context → a client-sent `role: "system"` message, confirming the backend 400s it via zod exactly as spec 16 documents, which this UI can never trigger since it only ever sends `user`/`assistant`), then cleaned up (soft-deleted the test bike). Also confirmed via `curl` that the new `/bikes/[bikeId]/assistant` route returns a `307` to `/login` with no session cookie, i.e. spec 01's gate correctly covers this new route like every other protected page. No interactive browser tool is available in this environment, so the actual click-through (message bubble wrapping at ~390px, Enter-to-send, the thinking-state animation, the reset-on-bike-switch behavior) is code-reviewed against the confirmed API contract and this app's established conventions, not literally observed — same standing limitation as every prior spec in this file. `yarn build` succeeds (all 8 routes compile, emits `/bikes/[bikeId]/assistant` as a new dynamic route); `yarn lint` exits 0 (same 5 pre-existing warnings as spec 13, none new — the one near-miss, the `set-state-in-effect` error described above, was avoided rather than suppressed).

- **2026-07-26 (spec 13 implementation)** — Built spec 13 (spending & mileage trend charts, v2 scope, client for backend spec 15) end-to-end. Added `recharts` (v3.10.1, confirmed React 19/Next 16 compatible, first charting library in this app). `components/(main)/Spending/type/spending.types.ts` gained `TMonthlySpending`/`TSpendingTrend`; new `SpendingTrendChart.tsx` does its own `useFetchData<TSpendingTrend>(["spending","trend",bikeId], .../spending-summary/trend?months=3)` and renders a Recharts `BarChart` (`totalSpending` per `targetMonth`) plus a `PieChart` donut of the most recent month's `categoryBreakdown` — both reuse the app's existing `--color-chart-1..5` tokens from `globals.css` rather than introducing new colors. `Spending.tsx` gained a fourth `"trend"` value on `TPeriod` and a fourth pill button; the existing month/year picker JSX and the single-period `useFetchData`'s `enabled` condition already naturally excluded `"trend"` (it matched none of their pre-existing conditions), so no extra exclusion logic was needed beyond the new render branch. Mirrored the same shape on the mileage side: `mileage.types.ts` gained `TMileageTrend` (reusing the existing `TMonthlySummary` for its array entries), new `MileageTrendTab.tsx` renders a `BarChart` of `totalDistanceKm` per `targetMonth`, and `Mileage.tsx`'s existing data-driven `tabs` array gained a `{key: "trends", label: "Trends"}` entry plus a render branch — a pure addition, no restructuring, exactly as the spec predicted. No new routes — both features fold into the existing `spending`/`mileage` pages as planned. Verified: `yarn build` succeeds (all 8 routes compile, full type-check); `yarn lint` exits 0 (same 5 pre-existing warnings, none new). Confirmed the real backend responses (`curl` against a running local `bikelog_server` with spec 15 already live) match `TSpendingTrend`/`TMileageTrend` field-for-field, including that a zero-activity month always comes back as a real zero-valued entry (never omitted), which is what lets both charts handle the empty-data case without any extra guard code. Also hit the two touched pages directly against this project's own already-running dev server (`localhost:5173` — a _different_, unrelated Next.js app was found squatting on the default port 3000 during this check and was correctly identified and left alone rather than mistaken for this project) with and without a valid session cookie: both returned `200` and behaved identically to the already-verified `/dashboard` route (gate withholds content client-side, no server crash). No interactive browser tool is available in this environment, so the actual chart rendering/click-through (tab switching, tooltip hover, phone-width layout) is code-reviewed against the confirmed data contract and this app's established mobile-first conventions, not literally viewed — same standing limitation as every prior spec in this progress tracker.

- **2026-07-21 (spec 12 implementation)** — Built spec 12 (bike accessory) end-to-end: `components/(main)/BikeAccessory/type/bike-accessory.types.ts` (`TBikeAccessory`/`TCreateBikeAccessoryPayload`/`TUpdateBikeAccessoryPayload`/`TBikeAccessoriesApiResponse`), `BikeAccessoryFormModal.tsx` (`FuelLogFormModal`-style inline `defaultValues`, `name`/`urgency`/`status` all shown on both create and edit, `urgency`/`status` via `ControlledSelectField` with static hardcoded option arrays — no fetched-catalog precedent applied since these are fixed enums, not a live catalog), `BikeAccessoryCard.tsx` (name + urgency pill + status pill side by side, Edit/Delete-only action row — no third action since this module has no dedicated status endpoint, unlike spec 11's `bikeIssue`), `BikeAccessory.tsx` (list page — two independent `Select` filters, status and urgency, both using an `"all"` sentinel value like spec 11's filter, both included in the query key and both resetting `page` to 1 on change). New route `app/(main)/bikes/[bikeId]/accessories/page.tsx`. Added an "Accessories" tile (`ShoppingBag` icon) to `BikeDetailPage.tsx`'s `links` array, now 6 entries alongside spec 11's "Issues" tile. Verified live against the running `bikelog_server` via `curl` (register → login → create bike → create accessory without `status`, confirming it defaults to `"pending"` → attempt create without `urgency`, confirming the server itself 400s it as required, matching the form's `isRequired` → create a 2nd accessory → list, confirming `{result, meta}` shape → combined `?status=&urgency=` filter, confirming it narrows correctly → `PATCH /:id` status freely walked `pending → purchased → pending → cancelled` with no guard at any step, confirming this module has no state machine unlike spec 11's `bikeIssue` → delete → list again, confirming the deleted item drops out → a request against a nonexistent/unowned bike id, confirming it 404s via the shared `findOwnedBikeOrThrow` ownership check rather than leaking data), then cleaned up (soft-deleted all test data). No interactive browser tool available in this environment, so click-through UI (filter dropdown interaction, badge rendering) is code-reviewed against the confirmed API contract, same approach as spec 11 and every prior spec. `yarn lint` exits 0 (same 5 pre-existing warnings as spec 11, none new), `yarn build` succeeds and emits `/bikes/[bikeId]/accessories` as a new dynamic route. Both post-MVP frontend specs (11, 12) are now complete, matching the backend's post-MVP scope (`bikelog_server` specs 10/11).

- **2026-07-21 (spec 11 implementation)** — Built spec 11 (bike issue) end-to-end: `components/(main)/BikeIssue/type/bike-issue.types.ts` (`TBikeIssue`/`TCreateBikeIssuePayload`/`TUpdateBikeIssuePayload` with no `status` field/`TUpdateBikeIssueStatusPayload`/`TBikeIssuesApiResponse`), `BikeIssueFormModal.tsx` (title/description/dateReported only — `FuelLogFormModal`-style inline `defaultValues`, no status field at all), `BikeIssueCard.tsx` (title + status pill, description, date, three-icon action row: Edit/status-toggle/Delete), `BikeIssue.tsx` (list page — plain shadcn `Select` status filter using an `"all"` sentinel since Radix `SelectItem` rejects an empty-string value, `usePatch` wired to the dedicated `/:id/status` endpoint for the toggle, kept fully separate from the generic edit mutation). New route `app/(main)/bikes/[bikeId]/issues/page.tsx`. Added an "Issues" tile (`AlertTriangle` icon) to `BikeDetailPage.tsx`'s `links` array, now 5 entries (6 once spec 12 adds its own tile). Confirmed via `find`/`ls` before starting that the real on-disk structure is `components/(main)/<Domain>/`, not `components/feature/<domain>/` — this progress tracker's own prose (written before that restructuring) still describes the old path in its Spec 01-08 entries above; trust the code and `CLAUDE.md` over this file's older prose on folder layout specifically. Verified live against the running `bikelog_server` via `curl` (register → login → create bike → create two issues, confirming a client-sent `status: "resolved"` on create is correctly ignored and forced to `"open"` → list with and without `?status=` filter, confirming `{result, meta}` shape with `meta` as a raw count → generic `PATCH /:id` on title/description, confirming `status` is unaffected → `PATCH /:id/status` open→resolved→open → same-status re-toggle correctly 400s with `"Issue is already resolved"`, confirming the frontend's toggle button can never trigger that guard since it only ever offers the opposite status → delete, confirming the deleted issue drops out of the next list call), then cleaned up (soft-deleted the test issue and bike). No interactive browser tool available in this environment, so the click-through UI (button clicks, pill rendering, filter dropdown interaction) is code-reviewed against the confirmed API contract, not literally clicked through — same approach as every prior spec. `yarn lint` exits 0 (5 warnings, all pre-existing and none in files this spec touched — one of the five, `fuelLog/FuelLog.tsx`'s unused `isPending`, wasn't previously catalogued in Known Gaps, added below), `yarn build` succeeds and emits `/bikes/[bikeId]/issues` as a new dynamic route.

- **2026-07-20 (same day, spec 10 implementation)** — Built spec 10 (audit fixes) end-to-end: a fix pass over specs 04-08, triggered by a prior 3-way parallel audit that checked every spec 04-08 implementation file against its own spec's contract and the real `bikelog_server` source, not just spec prose. Re-verified all 15 findings independently before fixing (re-read every touched file plus the relevant backend service/controller code), then live-verified the fixes against a running `bikelog_server` instance via `curl` (register/login, create bike, 12 fuel logs including a full-tank fill that closes a mileage record, a maintenance log, and hits to `/reminders`, `/mileage/monthly`, `/mileage/yearly`) since no interactive browser tool is available in this environment — same approach as specs 01-03. Test data was cleaned up (soft-deleted) afterward.
  - **9 functional bugs fixed**: (1-2) fuel-log and maintenance-log delete both had a stale-closure bug — `useDelete*Log(bikeId, editingX?._id ?? "")` baked the target id into a hook-construction argument sourced from component state set in the same click handler that then immediately awaited the delete, so the id used was always one render behind (empty on the first-ever click). Fixed by moving the id from a hook-construction argument to a call-time argument of the returned function in `useFuelLogs.ts`/`useMaintenanceLogs.ts`, and updating both pages to call `deleteFuelLog(fuelLog._id)`/`deleteMaintenanceLog(log._id)` directly, dropping the now-unnecessary state juggling. Verified live: deleting a specific fuel log/maintenance log by id removed exactly that row. (3) `RemindersBanner.tsx` read `data?.data ?? []` then `.map()`, but the real `GET /bikes/:bikeId/reminders` response is `{ data: { reminders: [...] } }` — confirmed live via `curl`, exactly as described — so the object-not-array fallback never triggered. Fixed by reading `data?.data?.reminders ?? []` and retyping `useReminders` to `useFetchData<{ reminders: TReminder[] }>`. (4) The mileage-closed toast never fired because `FuelLogFormModal.tsx` read `response.mileageRecordClosed` instead of `response.data.mileageRecordClosed` (confirmed live: `POST .../fuel-logs`'s real envelope is `{ data: { fuelLog, mileageRecordClosed } }`); fixed the read and gave `useCreateFuelLog`'s return an explicit `Promise<TgenericResponse<TCreateFuelLogResponse>>` type so `tsc` would catch this class of mismatch in the future. (5) Fuel-log pagination was inert: backend `meta` (`Queryuilder.countTotal()`) is a raw document count (confirmed live: `12` for 12 logs), not the `{page, limit, total, totalPages}` object `TFuelLogsApiResponse` declared, and separately `page.tsx` passed its 1-based `page` state straight through to `GenericTableComponent`/`TableContent.tsx`, which treat `currentPage`/`onPageChange` as 0-based. Fixed by retyping `meta: number`, computing `totalPages = Math.ceil(meta / limit)` client-side, and converting at the boundary (`currentPage={page - 1}`, `onPageChange={(p) => setPage(p + 1)}`, explicit `itemsPerPage`). (6-7) Mileage Monthly/Yearly tabs used field names that don't exist on the backend's real `MonthlyMileageResult`/`YearlyMileageResult` shapes (confirmed live via `/mileage/monthly` and `/mileage/yearly`): `fuelLogs` should be `fuelLogCount`, and a fabricated `month: number` field (used as both the React key and in `new Date(year, month - 1)`) doesn't exist at all — only `targetMonth: "YYYY-MM"` does. Fixed the types and both tab components to use `fuelLogCount` and to derive the month index/key from parsing `targetMonth`. (8) `MaintenanceLogFormModal.tsx` never cleared the `oilType` RHF field when the selected maintenance type changed away from "Engine Oil" (the field just unmounted, RHF state persisted) — added an effect that calls `methods.setValue("oilType", "")` whenever `isEngineOil` is false. (9) None of `useCreateMaintenanceLog`/`useUpdateMaintenanceLog`/`useDeleteMaintenanceLog` invalidated `["reminders", bikeId]`, so the reminders banner went stale after any maintenance-log mutation — added that key to all three hooks' invalidation arrays; verified live that deleting the one seeded maintenance log correctly zeroed out `/reminders`.
  - **2 type-safety cleanups**: (10) `useMaintenanceLogs` (list hook) was typed `useFetchData<TMaintenanceLog[]>` against a real `{ result, meta }` response (confirmed live, `meta` is also a raw number here, same `Queryuilder.countTotal()` as fuel logs) — retyped to `useFetchData<{ result: TMaintenanceLog[]; meta: number }>` and dropped the `as unknown as` cast in `maintenance-logs/page.tsx`. (11) Removed the dead `["fuelLogs", bikeId, fuelLogId]` invalidation key from `useUpdateFuelLog` — confirmed no query anywhere uses that 3-segment key shape.
  - **Doc sync**: flipped `04-fuel-logs.md` through `08-spending-summary.md`'s own stale `Status: ⛔ Not started` lines to `✅ Complete` (they'd drifted out of sync with this file and `00-build-plan.md`, both of which already said Complete); added the 4th pre-existing lint warning and a newly-discovered `RemindersBanner` display gap to Known Gaps (see below); this spec's own status line and `00-build-plan.md`'s row 10 were flipped in progress → complete alongside this entry, per `ai-workflow-rules.md`'s Documentation Sync rule.
  - Verified: `yarn lint` exits 0 (same 4 pre-existing warnings, none new), `yarn build` succeeds (all routes compile, including the two dynamic `fuel-logs`/`maintenance-logs` pages touched here).
  - One gap found during live verification that's outside this audit's original 15 items, not fixed here (see Known Gaps): `RemindersBanner.tsx` renders a blank maintenance-type name for every reminder, because `getRemindersFromDB` never populates `maintenanceType` (confirmed live — it's a bare ObjectId string, not `{_id, name}`), and unlike `MaintenanceLogCard.tsx`, `RemindersBanner.tsx` has no defensive type check before reading `.name`.

- **2026-07-20 (same day, specs 07 + 08 implementation)** — Built spec 07 (maintenance logs & reminders) and spec 08 (spending summary) end-to-end.
  - **Spec 07**: Created `components/feature/maintenance-log/` with types (`TMaintenanceLog`/`TCreateMaintenanceLogPayload`/`TUpdateMaintenanceLogPayload`/`TReminder`), `useMaintenanceLogs.ts` (4 hooks: `useMaintenanceLogs`, `useCreateMaintenanceLog`, `useUpdateMaintenanceLog`, `useDeleteMaintenanceLog`, `useReminders`), `MaintenanceLogFormModal.tsx` (create+edit, `ControlledSelectField` for maintenance type, conditional oil type when "Engine Oil" selected, `DateSelect` for service date/next due date, comma-separated parts input, notes textarea), `RemindersBanner.tsx` (overdue red + upcoming amber reminder cards with km/days remaining), `MaintenanceLogCard.tsx` (stacked card with type, date, odometer, cost, interval, next due, service center, parts, notes, edit/delete). Maintenance logs page at `app/(main)/bikes/[bikeId]/maintenance-logs/page.tsx` (stacked cards, reminders banner, create/edit modals, delete with confirm). Added `RemindersBanner` to bike hub page (`app/(main)/bikes/[bikeId]/page.tsx`).
  - **Spec 08**: Created `components/feature/spending/` with types (`TSpendingSummary`/`TCategoryBreakdown`), `useSpendingSummary.ts` (`useFetchData` with period/month/year params and `enabled` guard), `SpendingSummaryView.tsx` (reusable display: large total at top, category breakdown list below). Spending page at `app/(main)/bikes/[bikeId]/spending/page.tsx` (tab switcher for month/year/lifetime, month input, year stepper, reusable summary view).
  - Verified: `yarn build` succeeds, `yarn lint` exits 0 (4 pre-existing warnings, unchanged).

- **2026-07-20 (same day, specs 05 + 06 implementation)** — Built spec 05 (mileage stats) and spec 06 (maintenance catalog) end-to-end.
  - **Spec 05**: Created `components/feature/mileage/` with `mileage.types.ts` (5 response types), `useMileage.ts` (4 hooks: `useMileageHistory`, `useMonthlyMileage`, `useYearlyMileage`, `useLifetimeMileage`), 4 tab components (`MileageHistoryTab` — stacked cards + approximate rolling-average card at top with muted "~approx" label; `MonthlyMileageTab` — native `<input type="month">` picker, client-computed average division with zero-division guard; `YearlyMileageTab` — prev/next stepper, grid of per-month cards; `LifetimeMileageTab` — single stats card with client-computed average). Mileage page at `app/(main)/bikes/[bikeId]/mileage/page.tsx` with `useState` tab switcher. History tab shows approximate figure visually distinguished from exact records; zero-division shows "—" instead of `NaN`; empty states handled gracefully. Verified: empty state renders, all four tabs load without data.
  - **Spec 06**: Created `components/feature/maintenance-type/` (types + `useMaintenanceTypes` + `MaintenanceTypeSection`) and `components/feature/engine-oil-type/` (types + `useEngineOilTypes` + `EngineOilTypeSection`). Both sections: inline create form (not modal — low frequency, simple fields) above a plain read-only list. `defaultIntervalKm`/`defaultIntervalDays` optional in the form. Settings page at `app/(main)/settings/catalog/page.tsx` stacking both sections vertically. Added a `Settings` gear icon in `AppShell`'s header nav (next to log out) to reach the catalog page. Verified: `yarn build` succeeds, `yarn lint` exits 0 (only 3 pre-existing warnings, unchanged).

- **2026-07-20** — Wrote the initial context doc set (`project-overview.md`, `architecture.md`, `code-standards.md`, `ai-workflow-rules.md`, `progress-tracker.md`, `ui-context.md`) and the 10-file spec plan (`context/specs/00` through `09`), based on: (1) `bikelog_server`'s completed API — cross-checked against the verified Postman collection at `bikelog_server/postman/` built earlier the same session, so every field/route referenced in these specs is confirmed against actual backend source, not guessed; (2) a full read-through of the inherited Next.js scaffold's shared components (`components/shared/input/*`, `Modal/*`, `table/*`) and utils (`hooks/useApi.ts`, `utils/axiosInstance.ts`, etc.) to document their real prop APIs and flag existing bugs (see Known Gaps below); (3) the format/structure of a separate, unrelated project's equivalent 6-file context set (`22 ref context for the frontend/`), used only as a structural/tone reference — no content copied, different product domain.
- **2026-07-20 (same day, follow-up)** — Revised the auth/login design in `architecture.md`, `code-standards.md`, `ai-workflow-rules.md`, and specs 01/02, at the developer's request to follow the login strategy of a prior reference project (`lib/tokenManager.ts`, `lib/axiosInstance.ts`, `hooks/useApi.ts` read directly for their exact implementation). Adopted: `cookies-next`-backed `tokenManager.ts` with proactive JWT-expiry checking, zod+`zodResolver` on the login/register forms specifically. `bikelog_server` has one token and no refresh endpoint, so the reference project's refresh-token queueing logic has no equivalent here — that entire code path is simply absent rather than built-and-unused.
- **2026-07-20 (same day, second follow-up)** — Extended the same instruction to the general API-calling/mutation layer (not just login), reading the reference project's `useApi.ts`/`api.ts` directly. Rewrote `hooks/useApi.ts`/`utils/api.ts` from the `table` scaffold's versions: `usePost`/`usePatch`/`useDelete` (renamed from the `table` scaffold's `useDeleteData`) take the endpoint URL as a call-site argument (`mutateAsync({url, payload})`) rather than baked into the hook; `useFetch_Legacy`/`useGet`/`useUpdateData` are dropped as unneeded. Domain hook files (`useBikes.ts` etc., planned in specs 03–08) are unaffected by name/ergonomics — they just wrap the new call shape internally.
- **2026-07-20 (same day, third follow-up)** — Three changes at once, per developer instruction: (1) documented that this Next.js web app is a v1 prototype, not the end-state — a React Native app is the actual main target, built next after this one proves the product out (`project-overview.md`'s Overview); (2) scoped v1 to the `user` role only — `bikelog_server`'s `userRole` field (`user`/`admin`) has no enforced authorization yet, so v1 builds zero admin functionality, but `app/(main)/` (v1) vs. `app/(admin)/` (reserved, empty, for later) is now the explicit route-group split across every doc that describes routing; (3) replaced the "interceptor-only, no guard" auth-protection design with a hard synchronous session gate at `app/(main)/layout.tsx` (`getToken()` check before rendering children, redirect to `/login` if absent) plus `/` unconditionally redirecting to `/login` — per the explicit requirement that no route besides `/login`/`/register` is reachable while logged out. Also removed every named reference to the external reference project used for the two auth/API-layer follow-ups above (it's being deleted from the workspace) — the patterns themselves are kept, just no longer attributed to it by name anywhere in this doc set.
- **2026-07-20 (same day, spec 01 implementation)** — Built spec 01 end-to-end. Deleted the dead scaffold (`ControlledTipTapTextEditor/`, all of `components/main/`, `app/table/`, `app/table2/`, `app/input/`, `app/multi-form/`, `app/api/rooms/`, duplicate `useDebounce.tsx`, `utils/GetCookies.ts`, `utils/constants/storageKey.ts`, plus the now-empty `app/api/`/`utils/constants/` dirs) and its dependencies (`@tiptap/*`, `gsap`, `@gsap/react`, `js-cookie`, `@types/js-cookie`), added `cookies-next`/`jsonwebtoken`/`@types/jsonwebtoken`. Built `lib/tokenManager.ts`, rewrote `utils/axiosInstance.ts` (proactive-expiry request-interceptor redirect + 401 response-interceptor redirect, no refresh flow), rewrote `utils/api.ts`/`hooks/useApi.ts` to the documented 4-hook shape. Built `components/layout/AppShell.tsx` (top bar + single-item bottom tab bar — only a Dashboard tab exists since no bike-context pages are built yet) and `app/(main)/layout.tsx`'s hard session gate; `app/page.tsx` now unconditionally redirects to `/login` via a server-side `redirect()` (chosen over a client-side redirect for that one file specifically, since it needs no auth check at all and a server redirect is both simpler and avoids any flash). One deviation from the spec's literal wording: the gate's `setAuthorized(true)` call sits inside `useEffect`, which a newer `eslint-plugin-react-hooks` rule (`react-hooks/set-state-in-effect`) flags by default; suppressed with a targeted `eslint-disable-next-line` plus an inline reason, since deferring the cookie read to post-mount is required here to avoid a server/client hydration mismatch (the alternative — reading the cookie during initial render — would make the server-rendered and first client-rendered output disagree). Verified: `yarn lint` exits 0 (3 pre-existing warnings remain in untouched `components/common/GenericTable.tsx`/`components/shared/table/GenericTableComponent.tsx`/`TableToolbar.tsx`, not introduced by this spec), `yarn build` succeeds, `yarn dev` + `curl /` confirmed the `307` redirect to `/login` with no console errors, grep confirms no dangling references to anything deleted. Two Verify-checklist items (the protected-route redirect and expired-JWT redirect, both requiring an actual page under `app/(main)/`) can't be exercised live yet since no `page.tsx` exists there until spec 03 — the gate logic is in place and will be re-verified live once that page exists.
- **2026-07-20 (same day, spec 02 implementation)** — Built spec 02 end-to-end. Added `components/feature/auth/auth.schema.ts` (`loginSchema`/`registerSchema`, verbatim from the spec), `LoginForm.tsx`/`RegisterForm.tsx` (`FormProvider` + `zodResolver`, inline `usePost()`, no `useAuth.ts` wrapper per the documented auth exception, show/hide password toggle via local `useState` + lucide `Eye`/`EyeOff`), `app/login/page.tsx`/`app/register/page.tsx` (thin wrappers, each with the already-logged-in mount-check redirect), and a logout button in `AppShell` (`clearToken()` + `router.replace("/login")`). Extended `components/shared/input/ControlledInput.tsx` with an optional `rightElement` prop (in place, not forked) since it had no icon slot for the password toggle. Two deliberate deviations from the spec's literal code snippets, both necessary corrections rather than scope changes: (1) used `useRouter().replace()` instead of the imported `redirect` from `next/navigation` for every post-mutation/post-mount navigation — `redirect()` is documented as unsupported outside of render (Server Components/Route Handlers/Server Actions), and the spec's own snippets call it from inside `setTimeout`/`useEffect` callbacks, which would throw an uncaught error rather than navigate; `router.replace()` is the correct API for client-triggered navigation and was already what spec 01's own gate used. (2) Read the error message via `error.message` instead of `error.response.data.message` — `utils/axiosInstance.ts` (spec 01) already unwraps every axios error into a flat `{statusCode, message, errors}` object before it reaches a mutation's catch block, so `.response` never exists on the error these forms actually see; keeping the literal path would have silently always shown the generic fallback, which is exactly what this spec's own Verify checklist warns against. `catch (error: any)` was also typed as `catch (error)` + a narrow cast, since `yarn lint`'s `@typescript-eslint/no-explicit-any` rejected the spec's literal snippet. Verified live against the running `bikelog_server` via direct `curl` calls (register → 200 with no password field in the response, duplicate register → 409, login → clean top-level `token` field, wrong password → 403 with a clean `message: "Password don't match !!"` that reaches the toast correctly through the adjusted `error.message` path) — no interactive browser tool is available in this environment, so the click-through flows (password toggle, redirect timing, cookie set in DevTools) are code-reviewed but not literally clicked through; `yarn lint` exits 0 (same 3 pre-existing warnings as spec 01, none new), `yarn build` succeeds and now emits `/login`/`/register` as static routes, `yarn dev` compiled both pages with no console errors. The logout button and the "visiting a protected `app/(main)/...` URL" gate behavior remain untestable in a real browser until spec 03 adds the first `page.tsx` under `app/(main)/` — same blocker noted in spec 01.
- **2026-07-20 (same day, spec 03 implementation)** — Built spec 03 end-to-end. Added `components/feature/bike/bike.types.ts`, `useBikes.ts` (all five hooks matching `architecture.md`'s documented shape exactly, invalidating `["bikes"]`/`["bikes", bikeId]`), `BikeFormModal.tsx` (`BaseModal` + `FormActionButtons`, `ControlledInput` fields, `purchaseDate` via a `Controller`-wrapped `DateSelect` since it's not natively RHF-integrated, `currentOdometer` only rendered in create mode and typed out of `TUpdateBikePayload` entirely), `BikeCard.tsx`, `app/(main)/dashboard/page.tsx` (`grid-cols-1 sm:grid-cols-2` card grid, not `GenericTableComponent`, per the spec's explicit reasoning that a data table is the wrong shape for "pick which bike to open" on a phone), and `app/(main)/bikes/[bikeId]/page.tsx` (hub page: info card, edit/delete affordances, four link cards to routes that don't have pages yet). One implementation-level decision not spelled out verbatim in the spec: `BikeFormModal`'s `useForm` uses `defaultValues` (computed from the optional `bike` prop), not RHF's `values` option — `values` re-syncs on every render where the object reference changes, and since a fresh object literal was being passed each render, that would have reset the form on every keystroke. `defaultValues` is safe here specifically because Radix's `Dialog.Content` unmounts its children whenever `open` is `false` (confirmed by reading `components/ui/dialog.tsx` — no `forceMount`), so the form component genuinely remounts fresh every time the modal opens, which is exactly when `defaultValues` is meant to be read. No zod schema for this form, matching `code-standards.md`'s Error Handling section — the login/register zod exception doesn't extend to bike (or any other domain) forms, so validation here is the same opportunistic `isRequired` pattern as every other non-auth form will use. Verified live against the running `bikelog_server` via `curl`: full create → list → get-by-id → update (confirmed `currentOdometer` silently ignored server-side even when explicitly sent) → soft-delete cycle, every response field-for-field matching `TBike`. Also confirmed via `curl` that `/dashboard` and `/bikes/[bikeId]` — the first real pages under `app/(main)/` — return `200` with empty pre-hydration HTML when no session cookie is present, i.e. spec 01's gate is correctly withholding content on a real page for the first time (the actual client-side `router.replace("/login")` still can't be observed by `curl`, only its content-withholding effect). `yarn lint` exits 0 (same 3 pre-existing warnings, none new), `yarn build` succeeds and now emits `/dashboard` (static) and `/bikes/[bikeId]` (dynamic, due to the `[bikeId]` param) as routes, `yarn dev` compiled all three new/changed routes with no console errors.

## Known Gaps / Open Questions

- **Auth cookie is not httpOnly.** `bikelog_server` returns the JWT in the JSON body and never sets cookies itself, so the client sets a plain (non-httpOnly) `cookies-next` cookie after login, via `lib/tokenManager.ts`. This is a deliberate, documented tradeoff (see `ai-workflow-rules.md`'s Protected Files section) — acceptable for a single-user personal tool, would need revisiting before this app ever had untrusted multi-tenant users.
- **No refresh-token flow exists, by design** — `bikelog_server` has no refresh-token endpoint at all, so there's no refresh-queue/retry logic to build. An expired token just means the interceptor redirects to `/login` — re-login is the only path back.
- **Session gate is a plain cookie check, not a `GET /auth/me` round-trip, by design** — `app/(main)/layout.tsx` only checks `getToken()` presence/expiry synchronously; it doesn't verify the token against the backend before rendering. A stale-but-not-yet-expired token would pass the gate and only fail on the first real API call (handled by the response interceptor's 401 branch). Accepted tradeoff for build speed — see `architecture.md`'s Auth & Access Model.
- **`useApi.ts`'s mutation hooks take the URL per-call, not per-hook, by design** — `usePost()`/`usePatch()`/`useDelete()` (and, as of spec 15, `usePut()`) are generic (`mutateAsync({url, payload})`) rather than the more common "one hook per endpoint" shape. Domain hook files (`useBikes.ts` etc.) are what give call sites a fixed-endpoint convenience wrapper — see `architecture.md`'s example. **Update (spec 15): `apiPut`/`usePut` now exist** — `bikelog_server` gained real `PUT .../image` routes in its own spec 17, and this app's spec 15 added the matching client-side support (mirrors `apiPatch`/`usePatch`'s exact shape). The "no PUT routes" premise this bullet originally documented is no longer true; kept here with the correction rather than deleted, since the general "URL is a call-site argument, not baked into the hook" design point still stands.
- **`app/(admin)/` is empty and reserved, not a placeholder to fill in speculatively** — no admin spec exists yet. See `project-overview.md`'s "Roles" section.
- **Component bugs found in the `table`-scaffold inherited component library — fixed or avoided by design in spec 01:**
  - `hooks/useDebounce.ts` and `useDebounce.tsx` were byte-identical duplicates — the `.tsx` one is deleted (fixed in spec 01).
  - Several shared components (`TableFilter`, `TablePagination`, `DateSelect`, `BaseModal`) reference Tailwind classes (`table-border`, `primary-500`, `primary-600`) that didn't exist in `globals.css`'s `@theme` block — added in spec 01 (`--color-table-border`, `--color-primary-500`, `--color-primary-600`, all mapped onto existing tokens rather than a new hue).
  - `ControlledMultiSelectField`'s `onCreateOption` has a type bug (pushes a `Number` into a `string[]` field, no `undefined` guard) — Bike Log avoids this entirely by not using creatable multi-select anywhere (`partsReplaced` uses a plain text input instead, per spec 07). Not fixed, just avoided — the component itself still has the bug if some future spec reaches for it.
  - `PageHeader`/`StaticPageBreadcrumb` disagree on the dashboard route (`/dashboard` vs `/overview`) — standardized on `/dashboard` in this app's usage. Not yet fixed in the components themselves (no page uses either component yet); revisit whichever spec first wires up page headers/breadcrumbs.
- **Two more pre-existing warnings surfaced by spec 01's `yarn lint` run, not previously catalogued** — `components/common/GenericTable.tsx` and `components/shared/table/GenericTableComponent.tsx` both trigger a React Compiler "incompatible library" memoization warning on their `useReactTable()` call, and `components/shared/table/TableToolbar.tsx` has an unused `activeTab` prop. All three are warnings (not errors, `yarn lint` still exits 0) in files spec 01 didn't touch — leave for whichever spec first builds a table-based screen.
- **A 4th pre-existing warning, introduced by spec 07 but never individually catalogued** — `components/feature/maintenance-log/MaintenanceLogFormModal.tsx`'s `methods.watch("maintenanceType")` call triggers the same React Compiler "incompatible library" warning as the two `useReactTable()` call sites above (react-hook-form's `watch()` returns functions that can't be memoized safely). Spec 07's and spec 08's Recent Activity entries both said "yarn lint exits 0 (4 pre-existing warnings, unchanged)" without flagging that the 4th one was new as of spec 07 — corrected here per the spec 10 audit. `yarn lint` still exits 0; this is a warning, not an error, and not worth suppressing.
- **`RemindersBanner.tsx` renders a blank maintenance-type name for every reminder** — found during spec 10's live verification (not one of the audit's original 15 items). `GET /bikes/:bikeId/reminders` (`maintenanceLog.service.ts`'s `getRemindersFromDB`) queries with `.lean()` and never `.populate("maintenanceType")`, so each reminder's `maintenanceType` field is a bare ObjectId string at runtime, not the `{ _id, name }` shape `TReminder` declares and `RemindersBanner.tsx` reads (`r.maintenanceType.name`, `r.maintenanceType._id`). Unlike `MaintenanceLogCard.tsx` (which already guards with `typeof log.maintenanceType === "object"` before reading `.name`), `RemindersBanner.tsx` has no such guard, so it silently renders `undefined` instead of a name — not a crash, but a visible display bug. Not fixed as part of spec 10 (out of that audit's scope, and touching `bikelog_server/` is off-limits from this app per `ai-workflow-rules.md`'s Protected Files); flagging here for a future fix, either by populating `maintenanceType` server-side or by resolving the id client-side against the already-fetched `useMaintenanceTypes()` list (the same pattern `MaintenanceLogFormModal.tsx` already uses for `selectedMt`).
- **No interactive browser tool is available in this environment** — verification of both spec 01 and spec 02 relied on `curl`, `yarn build`/`yarn lint`/`yarn dev` output, and direct code review instead of literally clicking through the UI. Functionally-real checks (actual API calls, actual HTTP status codes/response shapes) were done via `curl` against the live `bikelog_server`, but things like "does the eye icon actually toggle the input type on click" or "is there a visible flash before redirect" are code-reviewed, not observed. Worth a manual pass in an actual browser once enough screens exist to be worth it.
- **`next/navigation`'s `redirect()` is not used anywhere in this app for client-triggered navigation, despite specs 01/02 both showing it in code snippets** — it only works during render (Server Components/Route Handlers/Server Actions), not from event handlers or `useEffect`/`setTimeout` callbacks. Every actual post-mount or post-mutation navigation uses `useRouter().replace()` instead (both `app/(main)/layout.tsx`'s gate and spec 02's login/register flows). `app/page.tsx`'s root redirect is the one exception — it's a Server Component doing a render-time `redirect()`, which is exactly where the function is meant to be used.
- **`ControlledInput` gained a `rightElement` prop in spec 02** (not part of the original inherited component) — pass a `ReactNode` (e.g. an icon button) to render inside the input on the right edge, used for the login/register password show/hide toggle. Any future spec needing a similar inline-icon input should reuse this prop rather than forking the component.
- **The four link cards on `/bikes/[bikeId]` (Fuel Logs, Mileage, Maintenance, Spending) point to routes that don't have pages yet** — `fuel-logs`/`mileage`/`maintenance-logs`/`spending` are specs 04/05/07/08. Clicking them currently 404s; this is expected and matches spec 03's own scope (it only builds the bike hub and the links out of it, not the destinations).
- **`BikeFormModal` uses RHF's `defaultValues`, not `values`, and this is load-bearing, not a stylistic choice** — see the spec 03 Recent Activity entry above for why `values` would have reset the form on every keystroke. Any future domain's create/edit modal following the same "one `FormModal` component, optional entity prop toggles edit mode" pattern (fuel logs, maintenance logs, etc.) should default to `defaultValues` too, and only reach for `values` if there's a genuine need to re-sync the form from data that can change _after_ mount without a remount.
- **No test suite planned**, matching the backend's own stance — manual verification only (see `code-standards.md`).
- **A 5th pre-existing lint warning, surfaced by spec 11's `yarn lint` run, not previously catalogued** — `components/(main)/fuelLog/FuelLog.tsx` destructures `isPending` from its `useDelete(...)` call but never reads it (`@typescript-eslint/no-unused-vars`). Predates spec 11, which didn't touch this file — just wasn't individually documented before now. `yarn lint` still exits 0; a warning, not an error.
- **`recharts` was added in spec 13** (the app's first charting library, previously "no charting library" was an explicit v1 scope decision — see `CLAUDE.md`'s "no global client state library, charting library, or animation library" line, now stale on the charting part specifically). `SpendingTrendChart.tsx`/`MileageTrendTab.tsx` are the reference for the pattern (Recharts `ResponsiveContainer` + the app's existing `--color-chart-1..5` CSS tokens from `globals.css`, not new colors) if a future spec adds another chart. Chart rendering itself (bar heights, donut segments, tooltip hover, phone-width clipping) is code-reviewed against the confirmed API data shape, not visually confirmed — no interactive browser tool is available in this environment, same standing limitation noted elsewhere in this file.
- **This file's own prose for specs 01-08 (the "Current Phase"/"Completed" sections above) still names the old `components/feature/<domain>/` folder layout** — confirmed via direct `ls`/`find` during spec 11 that the real on-disk structure is `components/(main)/<Domain>/` (matching `CLAUDE.md`'s explicit warning about `architecture.md`/`code-standards.md` being stale on this exact point). This file itself wasn't corrected at the time of the restructuring. Trust the code and `CLAUDE.md` over this file's older prose specifically on folder paths; the historical narrative (what was built, in what order, why) is still accurate.

## Next Up

**All specs through 22 are now complete.** Spec 17 (bike documents, client for backend spec 19 — note: unrelated to and not to be confused with this project's own spec 19, the decimal-field fix) was picked up and finished in a later session: the actual code (types, schema, `FileGalleryField.tsx`, form modal, card, list page, route, `BikeDetailPage.tsx` tile) was already fully written on disk from an earlier, uncommitted-to-docs pass, but its own file's checkboxes were still unchecked and it had never been build/lint/live-verified — this session did exactly that (see spec 17's own Recent Activity entry) and found the existing implementation correct with no fixes needed. No further spec is defined yet; next unit of work isn't otherwise decided.
